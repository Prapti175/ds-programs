#include<iostream>
using namespace std;
class node
{
    public:
    int data;
    node*next;
    node(int x)
    {
        data=x;
        next=NULL;
    }
};
class queue
{
    private:
    node*front;
    node*rear;
    public:
    queue()
    {
        front=NULL;
        rear=NULL;
    }
    int empty()
    {
        if(front==NULL)
        return(1);
        else
        return(0);
    }
    void enqueue(int x)
    {
        node*p=new node(x);
        if(front==NULL)
        {
            front=p;
            rear=p;
        }
        else
        {
            rear-> next=p;
            rear=p;
        }
    }
    int dequeue()
    {
        int x=front->data;
        node*p=front;
        front=front->next;
        delete p;
        return(x);

    }
    int peek()
    {
        return(front->data);
    }
    void display()
    {
        node*q=front;
        cout<<endl;
        while(q!=NULL)
        {
            cout<<"\t"<<q->data;
            q=q->next;
        }
    }
};

int main()
{
    int x;
    queue q;
     q.enqueue(50);
     q.enqueue(40);
     q.enqueue(30);
     q.enqueue(10);

     q.display();
     if(!q.empty())
     {
        x=q.dequeue();
        cout<<"removed:"<<x;
     }
     q.display();
     x=q.peek();
     cout<<"front element is:"<<x;
     return(0);
}

/*
                 > cd "d:\prapti_kale\" ; if ($?) { g++ queuelinked.cpp -o queuelinked } ; if ($?) { .\queuelinked }

        50      40      30      10removed:50
        40      30      10front element is:40
PS D:\prapti_kale>*/